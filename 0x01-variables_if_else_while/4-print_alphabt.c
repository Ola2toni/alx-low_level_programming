#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
{
	char alphabet;
	for (alphabet = 'a'; alphabet c = 'z'; alphabet++)
	{
		if (alphabet == 'a')
			continue;
		else if (alphabet == '0')
			continue;
		putchar (alphabet);
	}
	putchar ('\n');
	return (0);
}
